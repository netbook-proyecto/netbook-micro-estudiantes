server:
  port: 8082

spring:
  application:
    name: netbook-micro-estudiantes
  datasource:
    url: jdbc:mysql://localhost:3306/db_estudiantes?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/